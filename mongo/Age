// Check all unique age ranges in the collection
db.Patron.distinct("age_range")

// Check how many patrons are in specific age group labels
db.Patron.aggregate([
  { $group: { _id: "$age_group", count: { $sum: 1 } } },
  { $sort: { count: -1 } }
])

